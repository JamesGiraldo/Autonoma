<h1 class="text-center mov-naranja">LISTA DE <strong>CURSOS</strong></h1>
<hr />
<div class="row">
  <%= form_tag(cursos_path, class:"navbar-form-custom" , method: :get) do %>
    <div class="form-group">
      <input type="text" name="q" class="form-control" placeholder="Buscar..." type="text" value="<%= params[:q] %>">
    </div>
  <%end%>
</div><br>
<table class="table table-striped">
  <tr>
    <th class="active">NOMBRE</th>
  </tr>
  <%= render partial: "cursos", collection: @cursos, as: :curso %>
</table>
<hr />
<% if current_user.has_role? :Admin%>
  <!-- <%= link_to "Nuevo Curso", new_curso_path(@curso) , class:"btn-primary  full-width m-b"%>
  <%= link_to "Nuev Curso", new_curso_path(@curso) , class:"btn-primary  full-width m-b"%> -->
  <div class="botonContainer">
    <div class="botonSeguimiento">
      <%= link_to "Nuevo Curso", new_curso_path(@curso), class: "botonSeguimientoColor"%>
    </div>
    <div class="botonSeguimiento">
      <%= link_to "Nuevo Curso", new_curso_path(@tiponovedad), class: "botonSeguimientoColor"%>
    </div>
  </div>
<%end%>
